@model EncuestasV2.Models.encuaesta_periodo

@{
    ViewBag.Title = "Details";
    Layout = "~/Views/Shared/_Layout2.cshtml";
}

<div class="container-fluid" style="height:496px;">
    <h2>Detalle de Periodos</h2>

    <div>
        
        <hr />
        <dl class="dl-horizontal">
            <dt>
                <label class="control-label ">Descripción de Periodo</label>
            </dt>

            <dd>
                @Html.DisplayFor(model => model.periodo_desc)
            </dd>

            <dt>
                <label class="control-label ">Estatus</label>
            </dt>

            <dd>
                @Html.DisplayFor(model => model.periodo_estatus)
            </dd>

        </dl>
    </div>
    <p>
        @*@Html.ActionLink("Edit", "Edit", new { id = Model.periodo_id }) |
        @Html.ActionLink("Back to List", "Index")*@
        <a href="@Url.Action("Edit", "periodo", new { id = Model.periodo_id })" class="btn btn-warning" target="_blank"><i class="fas fa-edit"></i><span></span><b>Editar</b></a>
        |
        <a href="@Url.Action("Index", "periodo")" class="btn btn-primary" target="_blank"><i class="fas fa-undo-alt"></i><span></span><b> Lista de Periodos</b></a>
    </p>
    </div>